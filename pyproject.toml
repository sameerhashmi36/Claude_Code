[project]
name = "claude-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sameerhashmi36", email = "sameerhashmi1995@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain>=1.3.18",
    "langchain-anthropic>=1.7.0",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "seaborn>=0.13.2",
]

[project.scripts]
claude-code = "claude_code:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
